- [compile-check misses the Editor assembly](compile-check-misses-editor-assembly.md) — fixed the DOTween/firstpass false failure (ProjectReference, not a stale csproj); Editor folder still uncovered by default
- [Unity Editor open blocks scene edits](unity-editor-open-blocks-scene-edits.md) — with UnityLockfile present, wire scenes via an Editor menu command, never by editing .unity YAML
- [UI change didn't land? read the checklist](ui-change-didnt-land-checklist.md) — the project's most repeated failure, different cause each time; CLAUDE.md has the ordered checklist, measure the file before assuming
- [Scene serialized copies beat the prefab](scene-serialized-copies-beat-the-prefab.md) — a size held in both PanelPalette and a scene [SerializeField] silently reverts at runtime; delete the copy, don't sync it
- [CSV card edits need the From CSV sync](card-sheet-csv-edits-need-fromcsv-sync.md) — one Sync With Sheet menu item now, xlsx-only unless locked; hand-editing the CSV alone no longer reaches Unity
- [CardSheet enum name tables track the C# enums](cardsheet-enum-name-tables-track-cs-enums.md) — append to $script:StatusNames when adding a StatusType, or Glossary rows become Unknown(N) and silently revert
- [StartAssetEditing nulls generated refs](startassetediting-nulls-generated-refs.md) — asset generators must not batch imports; an empty default-size board means CurrentLevel is null
- [Never ?? or ?. a UnityEngine.Object](never-null-coalesce-unity-objects.md) — they keep fake-nulls; GetComponent ?? AddComponent never adds. Use TutorialWiring.Ensure&lt;T&gt;
- [Enemy brains never play defensive cards](enemy-brains-never-play-defensive-cards.md) — only Attack/Move/Summon intents exist; a Shield/Block card in an enemy deck is dead weight, ride defence on an attack card
- [TextureImporter.spritesheet removed in Unity 6](textureimporter-spritesheet-removed-unity6.md) — per-frame pivot writes through it silently no-op; use ISpriteEditorDataProvider
- [Sprite cells are mostly padding](sprite-cells-are-mostly-padding.md) — pack art fills 13-63% of its cell and sits off-centre; derive pivot and scale from the opaque pixels, never the frame rect
- [DrawImage DPI fakes sprite padding](drawimage-dpi-fakes-sprite-padding.md) — 300-DPI art drawn with DrawImage(img,x,y) shrinks to a third in the corner; the Dark UI icon pack is NOT padded
- [Status icon normalization was REVERTED](status-icon-optical-normalization.md) — icons use the untouched pack originals; the real bug was LayoutElement minWidth -1, not the art. Don't rebuild the normalizer
- [PowerShell EPPlus string/parse gotchas](powershell-comma-binds-tighter-than-arithmetic.md) — `Cells[$row + 1, $col]` misparses; `"$c: text"` misparses; `Cells[$r,2]` as a nested call argument fails to parse
- [Enemy/Level design sheets](enemy-level-design-sheets-architecture.md) — Tools/EnemySheet + Tools/LevelSheet mirror Tools/CardSheet's sync pattern; key design choices and where the sheets' source of truth now lives
- [Totem gems and flat modifiers](totem-gem-and-flat-modifier-design.md) — gem shape = mechanic, colour = subject; Strength/Block hardcode AmountPerHit 3, so FlatDamage/GainBonus are the only authorable damage and shield numbers
